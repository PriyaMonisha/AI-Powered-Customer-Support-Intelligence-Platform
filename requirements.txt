# Customer Support Intelligence Platform — requirements.txt
# Install into venv: python -m pip install -r requirements.txt
# NEVER use bare `pip install` — on this machine pip → Python 3.14, python → Python 3.11

# ── Data & ML ───────────────────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.2
scipy==1.13.1

# ── Gradient Boosting ────────────────────────────────────────────────────────
xgboost==2.0.3
lightgbm==4.3.0

# ── NLP & Text ───────────────────────────────────────────────────────────────
spacy==3.7.5
nltk==3.8.1
vaderSentiment==3.3.2
wordcloud==1.9.3

# ── Transformers & PyTorch (CPU wheel for local dev; GPU in Colab) ───────────
torch==2.3.0
transformers==4.41.2
tokenizers==0.19.1
huggingface-hub==0.23.4
datasets==2.20.0
tenacity==8.3.0               # retry logic for HF Hub upload

# ── Experiment Tracking ──────────────────────────────────────────────────────
mlflow==2.13.2
optuna==3.6.1

# ── Explainability ───────────────────────────────────────────────────────────
shap==0.45.1

# ── Feature Store (custom Redis) ─────────────────────────────────────────────
redis==5.0.4

# ── Data Validation ──────────────────────────────────────────────────────────
pandera==0.20.3               # NOT Great Expectations (Windows MAX_PATH issue)
sqlalchemy==2.0.30
psycopg2-binary==2.9.9

# ── Monitoring & Drift ───────────────────────────────────────────────────────
evidently==0.4.30             # PINNED — breaking API change between v0.2 and v0.4
prometheus-client==0.20.0

# ── API ──────────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.3.1
python-multipart==0.0.9

# ── Dashboard ────────────────────────────────────────────────────────────────
dash==2.17.1
dash-bootstrap-components==1.6.0
plotly==5.22.0

# ── Visualization (static charts — NOT Plotly write_image/Kaleido) ───────────
matplotlib==3.10.9
seaborn==0.13.2

# ── Workflow ─────────────────────────────────────────────────────────────────
# apache-airflow installed separately in docker/airflow/requirements.txt
# (too heavy + OS-specific for dev venv)

# ── Dev & Testing ────────────────────────────────────────────────────────────
pytest==8.2.2
pytest-cov==5.0.0
pytest-asyncio==0.23.7
httpx==0.27.0                 # for FastAPI TestClient
black==24.4.2
flake8==7.0.0
python-dotenv==1.0.1

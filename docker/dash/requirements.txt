# Customer Support Intelligence Platform — docker/dash/requirements.txt
# Trimmed subset for the Dash dashboard image — every line pinned to the exact
# version in root requirements.txt (or, for transitive deps not listed there,
# the version installed in the dev venv) to avoid unpickle-version mismatches
# when loading TabularEncoder/sklearn artifacts. No torch/transformers/xgboost/
# lightgbm — dash_app never imports them.

dash==2.17.1
dash-bootstrap-components==1.6.0
plotly==5.22.0
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.2
joblib==1.5.3
requests==2.34.2
mlflow==2.13.2
matplotlib==3.10.9
python-dotenv==1.0.1
gunicorn==22.0.0
